#include "main.h"
/**
 * _abs - computes the absolute value of an integer.
 * @n: the integer to compute.
 * Return: the absolute value if intf=eger n.
*/
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
